---
phase: 02-learning-algorithm-integrity
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/learning.py
  - backend/tests/test_learning.py
  - backend/tests/test_global_aggregation.py
autonomous: true
requirements: [BUGFIX-04, LEARN-01]
must_haves:
  truths:
    - "Submitting a ride segment for a never-seen (segment_id, bin_id) creates a segment_stats row and counts the observation as accepted (n becomes 1) — never rejected with missing_stats (BUGFIX-04, D-09/D-11)"
    - "When the segment has zero existing segment_stats rows, the seed uses schedule_mean = 0.0 (AVG returns NULL) (BUGFIX-04, D-10)"
    - "update_ema, compute_time_based_alpha, and is_stale no longer exist in app.learning (LEARN-01 D-03, D-07)"
    - "update_segment_stats no longer reads or writes ema_mean/ema_var and no longer calls the EMA helpers (LEARN-01 D-02)"
    - "missing_stats is fully retired as a rejection reason from update_segment_stats"
  artifacts:
    - "backend/app/learning.py with a seed-and-fall-through update_segment_stats and the three EMA/is_stale functions deleted"
    - "backend/tests/test_learning.py with EMA tests deleted and removal-verification + seed tests added"
    - "backend/tests/test_global_aggregation.py rejection tests updated for post-BUGFIX-04 behavior"
  key_links:
    - "D-02 (drop ema_mean/ema_var from the SELECT/UPDATE in update_segment_stats) and D-09/D-11 (seed-and-fall-through) edit the SAME SQL statements — they must land together (RESEARCH.md Pitfall #4)"
    - "D-02 (remove call sites) and D-03 (delete definitions) MUST be in the same commit or learning.py raises NameError on the next ride submission (RESEARCH.md Pitfall #4, critical_note #1)"
---

<objective>
Perform the core `learning.py` surgery converging BUGFIX-04 and LEARN-01, all landing on or around `update_segment_stats`:

1. BUGFIX-04 (D-09/D-10/D-11): Replace the `if row is None: return False, "missing_stats"` block with seed-and-fall-through — INSERT a new segment_stats row (n=0, welford_mean=seed, welford_m2=0, schedule_mean=seed where seed = AVG(schedule_mean) across the segment's other bins, or 0.0 if none), then continue into the SAME outlier-check + Welford-update logic so the triggering observation is accepted (n becomes 1).
2. LEARN-01 (D-02): Stop reading/writing ema_mean/ema_var in update_segment_stats' SELECT and UPDATE, and remove the compute_time_based_alpha/update_ema call sites.
3. LEARN-01 (D-03/D-07): Delete update_ema, compute_time_based_alpha, and is_stale function definitions entirely.
4. BUGFIX-06 (D-12, this function only): Remove the trailing conn.commit() from update_segment_stats. (The other two commit removals + D-13 are Plan 04.)
5. Delete the now-orphaned EMA unit tests; add removal-verification and seed-behavior tests; update the two stale rejection tests in test_global_aggregation.py.

Purpose: New route segments must learn from their first observation (BUGFIX-04 is the highest-value user-facing fix in this phase), and EMA dead code must be removed per LEARN-01's "no silent dead code" wording. Per D-01, EMA is removed from the active pipeline now (incorporation into the blend is deferred to v2 / LEARN-V2-01) — this plan implements the removal half of that decision (D-02/D-03); the ema_mean/ema_var columns are retained inert per D-05 (no schema change).

Output: A rewritten update_segment_stats, three deleted functions, updated imports, new/updated tests.

CRITICAL sequencing (RESEARCH.md Pitfall #4, critical_note #1): D-02's call-site removal and D-03's definition deletion must be in one commit; if split, learning.py raises NameError on the next ride. The seed logic (D-09) and the EMA column removal (D-02) edit the same SELECT/UPDATE statements — do them as one coherent rewrite of update_segment_stats.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-learning-algorithm-integrity/02-CONTEXT.md
@.planning/phases/02-learning-algorithm-integrity/02-RESEARCH.md
@.planning/phases/02-learning-algorithm-integrity/02-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — seed-and-accept tests + EMA/is_stale removal-verification tests</name>
  <files>backend/tests/test_learning.py</files>
  <read_first>
    - backend/app/learning.py:241-324 (current update_segment_stats — the function being rewritten; note the SELECT at 269-276 pulls ema_mean/ema_var, the missing_stats return at 279-283, the EMA call sites at 301-303, the UPDATE at 306-322, and the trailing conn.commit() at 323)
    - backend/app/learning.py:36-50 (update_ema), :134-156 (compute_time_based_alpha), :159-166 (is_stale) — the three deletion targets
    - backend/tests/test_learning.py:12-22 (import block — update_ema and compute_time_based_alpha imported here), :49-54 (test_ema_update), :132-153 (test_time_based_alpha)
    - backend/tests/conftest.py:91-113 (in_memory_db fixture — yields a sqlite3.Connection with schema loaded, for unit-level DB tests of update_segment_stats)
    - backend/app/schema.sql (segment_stats table — columns segment_id, bin_id, n, welford_mean, welford_m2, ema_mean, ema_var, schedule_mean, last_update; schedule_mean REAL NOT NULL DEFAULT 0.0)
    - .planning/phases/02-learning-algorithm-integrity/02-RESEARCH.md "Code Examples" (EMA-removal-verification test bodies) and Pattern 1 (seed-then-fall-through shape)
    - .planning/phases/02-learning-algorithm-integrity/02-PATTERNS.md update_segment_stats section (target diff shape) and the removal-verification test template
  </read_first>
  <behavior>
    - test_update_segment_stats_seeds_new_bin_and_accepts (uses in_memory_db): insert a bare segments row and one existing segment_stats row for bin 0 with schedule_mean=300.0; call update_segment_stats(conn, segment_id, bin_id=5, duration_sec=310.0, mapmatch_conf=1.0) for the UNSEEDED bin 5; assert it returns (True, None); assert a segment_stats row for bin 5 now exists with n == 1 and welford_mean == pytest.approx(310.0); assert its schedule_mean == pytest.approx(300.0) (seeded from AVG of the existing bin)
    - test_seed_falls_back_to_zero_when_no_existing_bins (uses in_memory_db): insert a bare segments row with NO segment_stats rows at all; call update_segment_stats for bin 5, duration_sec 250.0, mapmatch_conf 1.0; assert returns (True, None); assert the new bin-5 row has schedule_mean == 0.0 and n == 1 (D-10 fallback)
    - test_seed_row_is_not_falsely_rejected_as_outlier (uses in_memory_db): same bare-segment setup; the first observation on a seeded n=0 row must be accepted (is_outlier short-circuits at n<=5) — assert (True, None), never (False, "outlier")
    - test_update_ema_and_compute_time_based_alpha_and_is_stale_are_removed: import app.learning; assert not hasattr for update_ema, compute_time_based_alpha, is_stale
    - test_ema_columns_not_written_by_update_segment_stats: use inspect.getsource(update_segment_stats); assert the strings ema_mean, ema_var, "update_ema(", "compute_time_based_alpha(", and "missing_stats" are all absent from the source
  </behavior>
  <action>
    In `backend/tests/test_learning.py`: (1) remove `update_ema,` and `compute_time_based_alpha,` from the `from app.learning import (...)` block (lines 12-22) — leaving them causes an ImportError once the source functions are deleted in Task 2; (2) delete `test_ema_update` (lines 49-54) and `test_time_based_alpha` (lines 132-153) entirely (D-03); (3) add `import inspect` and, if not present, ensure `from app.learning import update_segment_stats` is available for the source-inspection test; (4) append the five tests in `<behavior>`. For the DB-backed seed tests, use the `in_memory_db` fixture and insert fixtures via `conn.execute`. Run the suite: the three removal-verification/source tests must FAIL (functions still exist / source still contains ema), and the seed tests must FAIL with the current `missing_stats` rejection. Commit with `test(02-02): add seed-and-accept + EMA-removal tests, delete EMA unit tests (BUGFIX-04, LEARN-01)`.

    NOTE (comment-text discipline): the source-inspection assertions negatively grep the string "missing_stats" inside update_segment_stats source. Do NOT leave the literal word missing_stats in any code comment inside update_segment_stats after Task 2. Reference it only in test assertion strings (a different file) and in this plan.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_learning.py -k "seeds_new_bin or falls_back_to_zero or not_falsely_rejected or are_removed or ema_columns_not_written" -v</automated>
  </verify>
  <acceptance_criteria>
    - The five new tests are collected without ImportError
    - test_update_ema_and_compute_time_based_alpha_and_is_stale_are_removed FAILS (functions still exist)
    - test_ema_columns_not_written_by_update_segment_stats FAILS (source still contains ema_mean)
    - test_update_segment_stats_seeds_new_bin_and_accepts FAILS with the current missing_stats behavior (returns (False, "missing_stats"))
    - test_ema_update and test_time_based_alpha no longer exist (`grep -c 'def test_ema_update\|def test_time_based_alpha' backend/tests/test_learning.py` returns 0)
  </acceptance_criteria>
  <done>Failing seed + removal-verification tests exist; orphaned EMA unit tests deleted; imports cleaned.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — rewrite update_segment_stats (seed + EMA removal + no-commit) and delete EMA/is_stale defs</name>
  <files>backend/app/learning.py</files>
  <read_first>
    - backend/app/learning.py (full file — this task edits update_segment_stats and deletes three functions)
    - .planning/phases/02-learning-algorithm-integrity/02-PATTERNS.md update_segment_stats target-shape section (the exact seed-and-fall-through block and the SELECT/UPDATE column changes) and the EMA/is_stale deletion section
    - .planning/phases/02-learning-algorithm-integrity/02-RESEARCH.md Pattern 1 (seed-then-fall-through) and Pitfall #4 (sequencing D-02 + D-03 in one commit)
  </read_first>
  <action>
    In `backend/app/learning.py`, perform ONE coherent rewrite (single commit) covering D-02, D-09, D-10, D-11, D-12, D-03, D-07:

    1. Shrink the SELECT in update_segment_stats to `SELECT n, welford_mean, welford_m2, schedule_mean, last_update` (drop ema_mean, ema_var) (D-02).
    2. Replace the `if row is None: ... return False, "missing_stats"` block with the seed-and-fall-through from PATTERNS.md: run `SELECT AVG(schedule_mean) FROM segment_stats WHERE segment_id = ?`, set seed_schedule_mean = that value or 0.0 if NULL (D-10), INSERT a row `(segment_id, bin_id, n=0, welford_mean=seed_schedule_mean, welford_m2=0.0, schedule_mean=seed_schedule_mean)`, then set the unpack tuple `n, welford_mean, welford_m2, schedule_mean, last_update = (0, seed_schedule_mean, 0.0, seed_schedule_mean, None)`. In the `else` branch unpack the found row into the same five variables. Do NOT return early (D-11).
    3. Remove the two EMA call-site lines (`alpha = compute_time_based_alpha(...)` and `ema_mean_new, ema_var_new = update_ema(...)`) (D-02).
    4. Change the UPDATE to `SET n = ?, welford_mean = ?, welford_m2 = ?, last_update = ?` (drop ema_mean, ema_var) with the params `(n_new, welford_mean_new, welford_m2_new, int(time.time()), segment_id, bin_id)` (D-02).
    5. Delete the trailing `conn.commit()` before `return True, None` (D-12 — this function only).
    6. Update the docstring's rejection_reason enumeration (line ~255) to `'low_mapmatch_conf', 'outlier', None if accepted` — remove 'missing_stats'. Do NOT leave the literal `missing_stats` anywhere in this function (comment-text discipline; test inspects source).
    7. Delete the three function definitions entirely: `update_ema` (36-50), `compute_time_based_alpha` (134-156), `is_stale` (159-166) (D-03, D-07). Remove the now-unused `Optional` from the import only if nothing else uses it (verify: compute_time_based_alpha and is_stale used Optional; check remaining usages before removing the import).

    Run the Task 1 tests — all must pass (GREEN). Commit with `fix(02-02): seed on first observation, remove EMA dead code from learning (BUGFIX-04, LEARN-01)`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_learning.py -v</automated>
  </verify>
  <acceptance_criteria>
    - All five Task 1 tests PASS
    - `grep -c 'def update_ema\|def compute_time_based_alpha\|def is_stale' backend/app/learning.py` returns 0
    - `grep -c 'missing_stats' backend/app/learning.py` returns 0
    - `grep -c 'ema_mean\|ema_var' backend/app/learning.py` returns 0
    - `grep -c 'conn.commit()' backend/app/learning.py` returns 2 (update_device_bucket and log_rejection still have theirs — removed in Plan 04)
    - `cd backend && uv run pytest tests/test_learning.py -v` — full unit module green
    - `cd backend && python -c "import app.learning"` (from backend/, via uv) does not raise
  </acceptance_criteria>
  <done>update_segment_stats seeds-and-accepts, has no EMA code, no missing_stats, no local commit; three dead functions deleted; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Update stale rejection tests in test_global_aggregation.py (Pitfall #3 regression)</name>
  <files>backend/tests/test_global_aggregation.py</files>
  <read_first>
    - backend/tests/test_global_aggregation.py:21-56 (global_agg_client fixture — seeds ONLY bin 0 for segment ROUTE_GLOBAL/STOP_X->STOP_Y; other bins are unseeded, so post-BUGFIX-04 they now seed-and-accept instead of missing_stats)
    - backend/tests/test_global_aggregation.py:227-284 (test_outlier_rejection — line 283 tolerates "missing_stats")
    - backend/tests/test_global_aggregation.py:312-358 (test_rejected_by_reason_breakdown — line 358 asserts `"missing_stats" in reasons or "outlier" in reasons`; the low_mapmatch_conf=0.60 segment still guarantees rejected_segments >= 1 and "low_mapmatch_conf" in reasons)
    - .planning/phases/02-learning-algorithm-integrity/02-RESEARCH.md Pitfall #3
  </read_first>
  <action>
    Update the two stale tests to reflect post-BUGFIX-04 behavior (missing_stats is no longer reachable for unseeded bins — those bins now seed-and-accept):

    1. test_rejected_by_reason_breakdown (line ~358): the third segment (default mapmatch_conf=1.0, an unseeded bin) is now seeded-and-accepted, so `missing_stats` will not appear. Replace the assertion `assert "missing_stats" in reasons or "outlier" in reasons` with `assert "low_mapmatch_conf" in reasons` (the 0.60-conf segment is the deterministic rejection). Keep `assert result["rejected_segments"] >= 1` — still true from the low-conf segment. If the test docstring/comments mention missing_stats, update them.
    2. test_outlier_rejection (line ~283): the conditional `if result["rejected_segments"] > 0:` block tolerates missing_stats. Since this test pre-seeds bin_id with n=10 (INSERT OR REPLACE), the segment IS seeded and the 400.0 duration triggers a genuine outlier. Tighten the assertion to `assert "outlier" in result["rejected_by_reason"]` and remove the `or "missing_stats"` branch — the seeded row means outlier is the only expected reason.

    Do NOT introduce the literal `missing_stats` into any comment; it may appear only in removed/replaced assertions. Commit with `test(02-02): update rejection tests for seed-and-accept behavior (BUGFIX-04)`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_global_aggregation.py -k "rejected_by_reason_breakdown or outlier_rejection" -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_global_aggregation.py -k "rejected_by_reason_breakdown or outlier_rejection" -v` PASSES
    - `grep -c 'missing_stats' backend/tests/test_global_aggregation.py` returns 0
    - `cd backend && uv run pytest tests/test_global_aggregation.py -q` — no new failures vs baseline
  </acceptance_criteria>
  <done>Both rejection tests assert deterministic reasons (low_mapmatch_conf / outlier); no missing_stats tolerance remains; test_global_aggregation.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (ride ingest) | Untrusted duration_sec/mapmatch_conf cross here; already validated upstream. segment_id and bin_id are server-derived, not client-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Denial of Service | BUGFIX-04 seed-row creation in update_segment_stats | low | accept | RESEARCH.md Security Domain confirms bin_id is server-computed via compute_bin_id() (bounded 0-191) and segment_id is validated against the segments table (routes.py:151-156) BEFORE update_segment_stats runs. The seed path creates at most 192 rows per legitimate segment — bounded by existing bin cardinality, not a new unbounded write. test_update_segment_stats_seeds_new_bin_and_accepts confirms the seeded bin_id stays in the valid range |
| T-2-03 | Tampering | EMA dead-code deletion | low | accept | Pure removal of unreferenced functions; grep confirmed zero callers outside update_segment_stats. No behavior a consumer depends on is removed (ema_mean/ema_var columns are retained inert per D-05) |

RESEARCH.md Security Domain (ASVS L1) found no applicable ASVS category. No high-severity threat; nothing to block on.
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_learning.py tests/test_global_aggregation.py -v` — green
- `cd backend && uv run pytest -n auto --dist loadfile -q` — full suite: note that test_api_v1_alignment config tests may now fail because config.py still has the fields but get_config is untouched here (config surface is Plan 03). Confirm any new failures are ONLY the config-alignment tests deferred to Plan 03, and no other regression appears. If test_integration.py::test_ride_submission_and_eta or any end-to-end ride test fails with NameError, D-02/D-03 were split — fix before completing.
</verification>

<success_criteria>
- BUGFIX-04 satisfied: a never-seen (segment_id, bin_id) seeds a row and the observation is accepted (ROADMAP criterion #1)
- LEARN-01 partially satisfied: EMA fully removed from learning.py (definitions + call sites + column reads/writes); config-surface completion is Plan 03
- No missing_stats rejection path remains; downstream rejection tests updated
</success_criteria>

<output>
Create `.planning/phases/02-learning-algorithm-integrity/02-02-SUMMARY.md` when done
</output>
