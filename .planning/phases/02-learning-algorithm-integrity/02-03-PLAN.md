---
phase: 02-learning-algorithm-integrity
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - backend/app/config.py
  - backend/app/models.py
  - backend/app/routes.py
  - backend/tests/test_api_v1_alignment.py
  - backend/tests/test_integration.py
  - docs/api.md
  - CLAUDE.md
  - docs/architecture.md
  - docs/PROJECT_STRUCTURE.md
  - docs/gtfs-database.md
autonomous: true
requirements: [LEARN-01]
must_haves:
  truths:
    - "GET /v1/config returns 200 (not 500) after the config fields are removed; ema_alpha and half_life_days are present in the response body with value null (D-14 soft-deprecate)"
    - "config.py Settings no longer defines ema_alpha or half_life_days (D-04)"
    - "ConfigResponse.ema_alpha and .half_life_days are Optional[...] = None with a DEPRECATED comment referencing LEARN-01 (D-14)"
    - "docs/api.md is updated spec-first to document ema_alpha/half_life_days as deprecated/nullable BEFORE the model/route change (CLAUDE.md Rule 1)"
    - "CLAUDE.md and architecture docs describe EMA as removed from the active pipeline (not deleted from history), framed as a v2 research item (D-06)"
  artifacts:
    - "backend/app/config.py without ema_alpha/half_life_days settings"
    - "backend/app/models.py with soft-deprecated ConfigResponse fields"
    - "backend/app/routes.py get_config passing None for both fields"
    - "updated test_api_v1_alignment.py and test_integration.py config assertions"
    - "docs pass across api.md, CLAUDE.md, architecture.md, PROJECT_STRUCTURE.md, gtfs-database.md"
  key_links:
    - "D-04 (remove Settings fields) and D-14 (get_config passes None, ConfigResponse Optional) MUST land in the same commit — otherwise get_config raises AttributeError: 'Settings' object has no attribute 'half_life_days' (RESEARCH.md Pitfall #1, critical_note #1/#2)"
    - "Depends on Plan 02: update_segment_stats' settings.half_life_days reader must already be removed, else deleting the Settings field breaks that function too"
---

<objective>
Complete LEARN-01's config-surface half: remove the now-orphaned `ema_alpha` and `half_life_days` settings from `config.py` (D-04) and soft-deprecate the corresponding `GET /v1/config` response fields (D-14) so the documented, unauthenticated endpoint keeps working (returns null, not 500) and no mobile client breaks.

Per critical_note #2 and D-14 (already confirmed with the user — do not re-litigate): soft-deprecate following this project's existing `timestamp_utc: Optional[int] = None  # DEPRECATED` convention. Update docs/api.md FIRST per CLAUDE.md Rule 1 (Spec-First), then models/routes, then relax the tests to accept null.

Also complete D-06's docs pass: correct "Welford + EMA" prose in the primary architecture/reference docs to state EMA is not currently in the active blend, framed as a v2 research item — do NOT erase EMA mentions entirely.

Purpose: D-04 without D-14 would 500 a live endpoint (Pitfall #1). This plan closes the config gap the research surfaced.

Output: config.py field removal, ConfigResponse soft-deprecation, get_config None args, updated config tests, docs pass.

CRITICAL sequencing (critical_note #1): D-04 (remove Settings fields) and D-14 (get_config None + ConfigResponse Optional) must be one commit to avoid an intermediate AttributeError state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-learning-algorithm-integrity/02-CONTEXT.md
@.planning/phases/02-learning-algorithm-integrity/02-RESEARCH.md
@.planning/phases/02-learning-algorithm-integrity/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec-first — update docs/api.md config section, then soft-deprecate + remove settings (D-14, D-04)</name>
  <files>docs/api.md, backend/app/models.py, backend/app/routes.py, backend/app/config.py</files>
  <read_first>
    - docs/api.md:1387-1428 (GET /v1/config response schema + field descriptions; lines 1397-1398 show half_life_days/ema_alpha example values, 1413-1414 their descriptions, 1523 a defaults note)
    - backend/app/config.py:14-17 (Settings fields — ema_alpha: float = 0.1 and half_life_days: int = 30 to delete; keep n0 and stale_threshold_days per D-07)
    - backend/app/models.py:4 (from typing import List, Optional — Optional already imported) and :140-153 (ConfigResponse)
    - backend/app/routes.py:434-446 (get_config ConfigResponse construction — half_life_days/ema_alpha kwargs at 437-438)
    - .planning/phases/02-learning-algorithm-integrity/02-PATTERNS.md ConfigResponse + get_config + config.py sections (exact target code)
    - .planning/phases/02-learning-algorithm-integrity/02-CONTEXT.md D-14 (authoritative resolution)
  </read_first>
  <action>
    Do these in order (Spec-First, CLAUDE.md Rule 1), ALL in one commit:

    1. docs/api.md FIRST: in the GET /v1/config section, change the example values at lines ~1397-1398 to `"half_life_days": null` and `"ema_alpha": null`; update the field descriptions (~1413-1414) to note both are DEPRECATED — EMA was removed from the active pipeline in Phase 2 (LEARN-01), retained as nullable for backward compatibility, pending v2 research (LEARN-V2-01); update the defaults note at ~1523 accordingly.
    2. backend/app/models.py: change ConfigResponse's `half_life_days: int` to `half_life_days: Optional[int] = None  # DEPRECATED: EMA removed from active pipeline, see LEARN-01` and `ema_alpha: float` to `ema_alpha: Optional[float] = None  # DEPRECATED: EMA removed from active pipeline, see LEARN-01`. Optional is already imported (models.py:4).
    3. backend/app/routes.py get_config: replace `half_life_days=settings.half_life_days` with `half_life_days=None` and `ema_alpha=settings.ema_alpha` with `ema_alpha=None`.
    4. backend/app/config.py: delete the `ema_alpha: float = 0.1` and `half_life_days: int = 30` lines. Keep n0, stale_threshold_days (D-07 leaves stale_threshold_days).

    Steps 2-4 must be one commit (Pitfall #1). Commit with `feat(02-03): soft-deprecate ema_alpha/half_life_days in GET /v1/config, remove settings (LEARN-01, D-04, D-14)`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_integration.py -k test_config_endpoint -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -c "from app.routes import get_config"` does not raise
    - `grep -c 'ema_alpha\|half_life_days' backend/app/config.py` returns 0
    - `grep -c 'settings.ema_alpha\|settings.half_life_days' backend/app/routes.py` returns 0
    - `grep -c 'Optional\[int\] = None.*DEPRECATED\|Optional\[float\] = None.*DEPRECATED' backend/app/models.py` returns 2
    - `grep -c 'null' docs/api.md` finds the updated config example (manual: the config JSON block shows half_life_days: null and ema_alpha: null)
    - test_config_endpoint (test_integration.py) still passes (it only asserts n0 and time_bin_minutes)
  </acceptance_criteria>
  <done>Settings fields removed; ConfigResponse soft-deprecated; get_config passes None; docs/api.md updated spec-first; endpoint returns 200 with null fields.</done>
</task>

<task type="auto">
  <name>Task 2: Relax test_api_v1_alignment.py config assertions to accept null (D-14)</name>
  <files>backend/tests/test_api_v1_alignment.py</files>
  <read_first>
    - backend/tests/test_api_v1_alignment.py:811-840 (test_get_config_has_all_spec_fields — required_fields dict at 819-831 requires half_life_days: int and ema_alpha: (int, float), plus presence + isinstance loops)
    - backend/tests/test_api_v1_alignment.py:843-859 (test_get_config_values_are_reasonable — asserts half_life_days == 30 at line 853 and 0.0 <= ema_alpha <= 1.0 at 854)
    - backend/tests/test_api_v1_alignment.py:862-871 (test_get_config_does_not_include_removed_fields — leave as-is; ema_alpha/half_life_days are NOT removed, they stay present-but-null)
    - .planning/phases/02-learning-algorithm-integrity/02-CONTEXT.md D-14 (relax to accept None)
  </read_first>
  <action>
    Update the two config tests so the soft-deprecated fields are still asserted present but allowed to be null:

    1. test_get_config_has_all_spec_fields: keep half_life_days and ema_alpha in the presence check (they must still be KEYS in the response per soft-deprecate), but move them out of the isinstance type-checking loop (or change their expected type to `type(None)`). Simplest: change the required_fields entries to `"half_life_days": (int, type(None))` and `"ema_alpha": (int, float, type(None))` so the existing isinstance loop accepts null. Keep the presence assertion (`field in data`) covering both.
    2. test_get_config_values_are_reasonable: replace the hard assertion `assert data["half_life_days"] == 30` with `assert data["half_life_days"] is None` and `assert 0.0 <= data["ema_alpha"] <= 1.0` with `assert data["ema_alpha"] is None` (per D-14 — the values are now null).

    Do NOT touch test_get_config_does_not_include_removed_fields (those are different, genuinely-removed fields). Commit with `test(02-03): accept null ema_alpha/half_life_days in config alignment tests (D-14)`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_api_v1_alignment.py -k get_config -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/test_api_v1_alignment.py -k get_config -v` — all three get_config tests PASS
    - test_get_config_has_all_spec_fields still asserts half_life_days and ema_alpha are present as keys
    - `grep -c 'data\["half_life_days"\] is None\|data\["ema_alpha"\] is None' backend/tests/test_api_v1_alignment.py` returns 2
  </acceptance_criteria>
  <done>Config alignment tests accept null for the two soft-deprecated fields while still asserting their presence; all get_config tests green.</done>
</task>

<task type="auto">
  <name>Task 3: D-06 docs pass — reframe "Welford + EMA" as EMA-removed / v2 research item</name>
  <files>CLAUDE.md, .claude/CLAUDE.md, docs/architecture.md, docs/PROJECT_STRUCTURE.md, docs/gtfs-database.md</files>
  <read_first>
    - CLAUDE.md (grep for EMA / half_life / "Welford + EMA" — multiple mentions in the algorithm-details and key-algorithms sections)
    - .claude/CLAUDE.md (Architecture / Key Algorithms sections mention Welford+EMA)
    - docs/architecture.md, docs/PROJECT_STRUCTURE.md, docs/gtfs-database.md (grep each for EMA / ema_var / half_life)
    - .planning/phases/02-learning-algorithm-integrity/02-CONTEXT.md D-06 (exact framing: keep EMA mentioned, note it is NOT in the active blend, flag v2 needs research into the best algorithm for predicting bus schedules from mobile-submitted data)
  </read_first>
  <action>
    In each of CLAUDE.md, .claude/CLAUDE.md, docs/architecture.md, docs/PROJECT_STRUCTURE.md, docs/gtfs-database.md: correct prose that describes the LIVE algorithm as "Welford + EMA" to state that EMA is NOT currently part of the active blend (Welford + schedule blend only, as of Phase 2 / LEARN-01), while KEEPING a note that EMA exists in schema (ema_mean/ema_var columns retained inert per D-05) and is deferred to v2 (LEARN-V2-01) pending research into the most effective algorithm for predicting bus schedules from the mobile-app-submitted data. Do NOT delete every EMA mention (D-06 explicitly forbids erasure). For docs/gtfs-database.md, annotate the ema_var/ema_mean column reference as "retained but not written by the active pipeline (LEARN-01)". Leave historical docs (CHANGELOG, PRDs, SECURITY_REVIEW, ALIGNMENT_*) untouched — they are point-in-time records, not live-algorithm descriptions.

    Scope guard: do NOT touch dwell_stats descriptions (D-08 — out of scope). Commit with `docs(02-03): reframe EMA as removed-from-active-pipeline / v2 research item (LEARN-01, D-06)`.
  </action>
  <verify>
    <automated>grep -rln -i "welford + ema\|welford+ema" CLAUDE.md .claude/CLAUDE.md docs/architecture.md docs/PROJECT_STRUCTURE.md docs/gtfs-database.md; echo "exit-check-done"</automated>
  </verify>
  <acceptance_criteria>
    - No primary doc (CLAUDE.md, .claude/CLAUDE.md, docs/architecture.md, docs/PROJECT_STRUCTURE.md, docs/gtfs-database.md) still describes the ACTIVE algorithm as "Welford + EMA" without a clarifying note that EMA is removed/inert (manual read confirms reframing, not deletion)
    - At least one EMA mention remains in the docs (D-06: not erased) — `grep -rlc -i ema CLAUDE.md docs/architecture.md` returns matches
    - dwell_stats prose unchanged (`git diff --stat` shows no dwell-only edits)
  </acceptance_criteria>
  <done>Primary docs describe Welford + schedule-blend as the active algorithm with EMA noted as inert/v2; EMA not erased; dwell_stats untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /v1/config | Unauthenticated public read of server config; response schema is a documented contract. Soft-deprecation keeps the schema backward-compatible (fields present, nullable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-04 | Information Disclosure | GET /v1/config field removal | low | accept | No secret is exposed or removed; the fields become null. grep confirmed zero mobile-client readers of these fields, so no consumer breaks. Soft-deprecate (present-but-null) is strictly more backward-compatible than removal |
| T-2-05 | Denial of Service | AttributeError 500 on GET /v1/config if D-04 lands without D-14 | low | mitigate | Enforced by same-commit sequencing (Task 1) + test_config_endpoint and test_get_config_* gates; get_config passes hardcoded None, never reads the deleted Settings attributes |

RESEARCH.md Security Domain (ASVS L1) found no applicable ASVS category. No high-severity threat; nothing to block on.
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_integration.py tests/test_api_v1_alignment.py -k "config" -v` — green
- `cd backend && uv run pytest -n auto --dist loadfile -q` — full suite; the config-alignment failures that Plan 02 may have introduced are now resolved. No new regressions vs baseline.
</verification>

<success_criteria>
- LEARN-01 fully satisfied together with Plan 02: EMA removed from code AND config surface, GET /v1/config still returns 200
- D-14 satisfied: fields soft-deprecated (present, null), spec-first docs updated, tests relaxed
- D-06 satisfied: docs reframe EMA as inert/v2 without erasing it
</success_criteria>

<output>
Create `.planning/phases/02-learning-algorithm-integrity/02-03-SUMMARY.md` when done
</output>
