---
phase: 04-data-management
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-04]
files_modified:
  - docs/deploy.md
autonomous: false
requirements: [DATA-01, DATA-04]

must_haves:
  truths:
    - "docs/deploy.md documents apply_migrations.sh as a manual upgrade-path step, separate from fresh bootstrap (D-04)"
    - "docs/deploy.md documents update_gtfs.sh usage and the scoped sudoers drop-in granting the bmtc user exactly systemctl stop/start bmtc-api"
    - "An operator has confirmed sudoers/sudo access for systemctl stop/start bmtc-api is configured on the target host before update_gtfs.sh's first production run"
  artifacts:
    - "docs/deploy.md (upgrade + GTFS-refresh + sudoers section)"
  key_links:
    - "sudoers rule scoped to the two exact command strings, no wildcard (V4, T-04-08)"
    - "apply_migrations.sh is for upgrading an existing DB; fresh bootstrap uses schema.sql + seeding (Pitfall 1) — documented as distinct paths"
---

<objective>
Document the manual operational steps the two new operator-triggered tools require, and gate the first production `update_gtfs.sh` run on a human confirmation that privileged service control is configured. Per D-04, `apply_migrations.sh` is invoked from documented deploy steps (not app startup); per RESEARCH.md Pitfall 3 / Open Question 2, the `bmtc` user needs a scoped sudoers entry to run `systemctl stop/start bmtc-api`, which is a host-config task that cannot be automated from the repo.

Purpose: make the DATA-01 upgrade path and the DATA-04 refresh workflow safely runnable in production, with the one genuinely non-automatable prerequisite (sudoers) verified by a human before first use.
Output: an updated `docs/deploy.md` and a blocking human-verify checkpoint.

## Artifacts this plan produces
- `docs/deploy.md` — MODIFIED: adds an upgrade section (apply_migrations.sh), a GTFS-refresh section (update_gtfs.sh), and the sudoers drop-in instructions
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-management/04-CONTEXT.md
@.planning/phases/04-data-management/04-RESEARCH.md

@docs/deploy.md
@backend/scripts/apply_migrations.sh
@backend/scripts/update_gtfs.sh
@backend/scripts/restore.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document upgrade path, GTFS refresh, and sudoers drop-in in docs/deploy.md</name>
  <files>docs/deploy.md</files>
  <read_first>docs/deploy.md (existing structure, install layout — confirm the deployed script path prefix, e.g. /opt/bmtc-api/scripts/), backend/scripts/apply_migrations.sh (from 04-01), backend/scripts/update_gtfs.sh (from 04-04), .planning/phases/04-data-management/04-RESEARCH.md (Common Pitfalls 1 & 3, Security Domain, Open Question 1)</read_first>
  <action>Add three additive sections to docs/deploy.md (do not rewrite existing content):

  1. "Upgrading an existing database" — per D-04, document that `apply_migrations.sh` is run MANUALLY after a code deploy that includes a new migration, and is distinct from fresh bootstrap. State the fresh-vs-upgrade distinction (RESEARCH Pitfall 1 / Open Question 1 recommendation): a fresh install uses `python -m app.bootstrap` (schema.sql already current, schema_migrations auto-seeded); an existing older DB is brought current by running `apply_migrations.sh`. Give the exact invocation with BMTC_DB_PATH.

  2. "Refreshing GTFS data" — document `update_gtfs.sh <path-to-new-gtfs.zip>` (D-10 local-path arg), the planned ~30-60s outage during the stop/clear/re-bootstrap window (D-15), that a pre-refresh backup is taken and auto-restored on validation failure (D-14), and that segment_stats/rides/ride_segments learning history is preserved (D-12).

  3. "Granting the bmtc user service-control permission" — per Pitfall 3 / Security Domain V4: document adding `/etc/sudoers.d/bmtc-gtfs-update` containing exactly the scoped rule granting `bmtc` NOPASSWD access to `systemctl stop bmtc-api` and `systemctl start bmtc-api` as two explicit fully-qualified command entries (no `systemctl *` wildcard — least privilege). Note the alternative of simply running `update_gtfs.sh` as root, and that each command+argument pair must be listed explicitly because sudoers does not glob.</action>
  <verify>
    <automated>grep -q 'apply_migrations.sh' docs/deploy.md && grep -q 'update_gtfs.sh' docs/deploy.md && grep -q 'sudoers.d' docs/deploy.md && grep -q 'systemctl stop bmtc-api' docs/deploy.md && grep -q 'systemctl start bmtc-api' docs/deploy.md</automated>
  </verify>
  <acceptance_criteria>docs/deploy.md has an upgrade section referencing apply_migrations.sh (manual, distinct from fresh bootstrap), a GTFS-refresh section referencing update_gtfs.sh with the outage/backup/rollback/learning-preservation notes, and a sudoers section with the scoped, wildcard-free rule.</acceptance_criteria>
  <done>Operators have exact, copy-pasteable instructions for the two new manual workflows and the sudoers prerequisite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2 (Checkpoint): Confirm scoped sudoers/service-control access on the target host</name>
  <action>BLOCKING human-verify checkpoint (not auto-approvable). Pause execution and ask the operator to confirm, on the target production host, that the scoped sudoers drop-in for `systemctl stop/start bmtc-api` is configured (or that the run-as-root fallback is accepted) before `update_gtfs.sh` is first run in production. See what-built / how-to-verify below.</action>
  <what-built>docs/deploy.md now specifies a scoped `/etc/sudoers.d/bmtc-gtfs-update` drop-in granting the `bmtc` user NOPASSWD rights to exactly `systemctl stop bmtc-api` and `systemctl start bmtc-api`. `update_gtfs.sh` (04-04) calls these commands (unless BMTC_SKIP_SERVICE_CONTROL=1). This prerequisite cannot be verified from the repo — it depends on the target host's actual sudoers/polkit state (RESEARCH Open Question 2).</what-built>
  <how-to-verify>
    On the target production host, confirm ONE of the following before the first real `update_gtfs.sh` run:
    1. The `/etc/sudoers.d/bmtc-gtfs-update` drop-in exists and is scoped to exactly the two commands with no wildcard — verify with `sudo -l -U bmtc` (should list `systemctl stop bmtc-api` and `systemctl start bmtc-api`, nothing broader), and `sudo visudo -c` reports the file is syntactically valid; OR
    2. The documented fallback is accepted: `update_gtfs.sh` will be run as root directly (`sudo bash update_gtfs.sh <zip>`).
    Also confirm there is no pre-existing broader sudoers rule (e.g. `systemctl *`) that would over-grant.
  </how-to-verify>
  <resume-signal>Type "approved" once the sudoers drop-in is in place (or the run-as-root fallback is accepted), or describe what needs to change in docs/deploy.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bmtc user → systemd | Non-root user granted narrowly-scoped privileged service control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege | /etc/sudoers.d/bmtc-gtfs-update | high | mitigate | Rule scoped to two exact fully-qualified command strings, no wildcard (V4 least privilege); human-verified via the blocking checkpoint before first production use; `sudo visudo -c` validates syntax. |
| T-04-12 | Spoofing | Broad pre-existing sudoers rule silently over-granting | medium | mitigate | Checkpoint explicitly asks the operator to confirm no broader `systemctl *` rule exists for the bmtc user. |

No package installs this phase — Package Legitimacy Audit N/A (RESEARCH.md).
</threat_model>

<verification>
- `grep -E 'apply_migrations.sh|update_gtfs.sh|sudoers.d' docs/deploy.md` — all present
- Blocking human-verify checkpoint approved (sudoers configured or run-as-root fallback accepted)
</verification>

<success_criteria>
The DATA-01 upgrade path and the DATA-04 GTFS-refresh workflow are fully documented for operators, and the one non-automatable prerequisite (scoped sudoers for service control) is human-confirmed before first production use.
</success_criteria>

<output>
Create `.planning/phases/04-data-management/04-05-SUMMARY.md` when done.
</output>
