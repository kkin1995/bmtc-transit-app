---
phase: 04-data-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/deploy/bmtc-rate-limit-cleanup.service
  - backend/deploy/bmtc-rate-limit-cleanup.timer
  - backend/deploy/bmtc-retention.timer
  - backend/tests/test_rate_limit_cleanup_script.py
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "bmtc-rate-limit-cleanup.timer fires daily at 00:15 and activates bmtc-rate-limit-cleanup.service, which runs the existing rate_limit_cleanup.sh"
    - "rate_limit_cleanup.sh deletes rate_limit_buckets whose last_refill is older than the 24h TTL and leaves fresh buckets intact"
    - "bmtc-retention.timer and bmtc-rate-limit-cleanup.timer have distinct explicit OnCalendar times (00:00 vs 00:15) so they never fire in the same instant (D-09)"
  artifacts:
    - "backend/deploy/bmtc-rate-limit-cleanup.service (new oneshot unit)"
    - "backend/deploy/bmtc-rate-limit-cleanup.timer (new timer unit, OnCalendar 00:15)"
    - "backend/deploy/bmtc-retention.timer (modified to explicit OnCalendar 00:00)"
    - "backend/tests/test_rate_limit_cleanup_script.py"
  key_links:
    - "service ExecStart points at the already-working backend/scripts/rate_limit_cleanup.sh (D-07, unchanged script)"
    - "timer stagger: retention 00:00, rate-limit-cleanup 00:15 (D-09, Pitfall 4)"
    - "units mirror bmtc-backup.service/.timer shape: Type=oneshot, EnvironmentFile=/etc/bmtc-api/env, WantedBy=timers.target"
---

<objective>
Wire the already-working but unscheduled `rate_limit_cleanup.sh` to a systemd timer (DATA-02, D-07), and give the two daily maintenance timers distinct explicit `OnCalendar=` times to avoid the bare-`daily` simultaneous-start footgun on the single-writer SQLite DB (D-09, RESEARCH.md Pitfall 4). Also add the first-ever subprocess-level test for `rate_limit_cleanup.sh`'s deletion behavior.

Purpose: bound `rate_limit_buckets` table growth on a schedule. The script already exists and works — this plan builds only the systemd glue it already documents expecting, plus a characterization test. Per D-08, `rate_limit_cleanup.sh` itself is NOT modified — its commented-out `VACUUM` block stays commented out (manual/future opt-in), so this plan touches only the unit files and a new test.
Output: two new systemd unit files, one modified timer, and a script-level test.

## Artifacts this plan produces
- `backend/deploy/bmtc-rate-limit-cleanup.service` — NEW `Type=oneshot` unit
- `backend/deploy/bmtc-rate-limit-cleanup.timer` — NEW timer, `OnCalendar=*-*-* 00:15:00`
- `backend/deploy/bmtc-retention.timer` — MODIFIED: `OnCalendar=daily` to `*-*-* 00:00:00`
- `backend/tests/test_rate_limit_cleanup_script.py` — NEW test module
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-management/04-CONTEXT.md
@.planning/phases/04-data-management/04-RESEARCH.md
@.planning/phases/04-data-management/04-PATTERNS.md

@backend/scripts/rate_limit_cleanup.sh
@backend/deploy/bmtc-backup.service
@backend/deploy/bmtc-backup.timer
@backend/deploy/bmtc-retention.timer
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rate-limit-cleanup systemd units + stagger retention timer (D-07, D-09)</name>
  <files>backend/deploy/bmtc-rate-limit-cleanup.service, backend/deploy/bmtc-rate-limit-cleanup.timer, backend/deploy/bmtc-retention.timer</files>
  <read_first>backend/deploy/bmtc-backup.service and backend/deploy/bmtc-backup.timer (exact unit shape to copy), backend/deploy/bmtc-retention.timer (current bare daily to replace), backend/scripts/rate_limit_cleanup.sh (its header comment names these exact timer filenames)</read_first>
  <action>Per D-07 and D-09, create the timer glue for the existing rate_limit_cleanup.sh.

bmtc-rate-limit-cleanup.service — copy bmtc-backup.service verbatim, changing Description to "BMTC API Rate Limit Bucket Cleanup" and the ExecStart line. Keep Type=oneshot, User=bmtc, Group=bmtc, EnvironmentFile=/etc/bmtc-api/env. For the ExecStart path, match the deployed convention: bmtc-backup.service uses /opt/bmtc-api/scripts/backup.sh, so use ExecStart=/opt/bmtc-api/scripts/rate_limit_cleanup.sh (same /opt/bmtc-api/scripts/ prefix as the existing backup unit — do NOT use the backend/scripts/ prefix from the RESEARCH sketch; the existing bmtc-backup.service prefix is authoritative). No [Install] section on the service — the timer owns activation.

bmtc-rate-limit-cleanup.timer — copy bmtc-backup.timer shape ([Unit] Description, [Timer] with Persistent=true, [Install] WantedBy=timers.target), but set OnCalendar to the explicit value *-*-* 00:15:00 (not bare daily/hourly) per D-09 / Pitfall 4.

Modify bmtc-retention.timer: change OnCalendar=daily to OnCalendar=*-*-* 00:00:00; leave everything else unchanged. This locks the two timers' 15-minute offset rather than leaving it incidental.</action>
  <verify>
    <automated>grep -q '00:15:00' backend/deploy/bmtc-rate-limit-cleanup.timer && grep -q 'rate_limit_cleanup.sh' backend/deploy/bmtc-rate-limit-cleanup.service && grep -q 'Type=oneshot' backend/deploy/bmtc-rate-limit-cleanup.service && grep -q '00:00:00' backend/deploy/bmtc-retention.timer && grep -q 'WantedBy=timers.target' backend/deploy/bmtc-rate-limit-cleanup.timer</automated>
  </verify>
  <acceptance_criteria>Both new unit files exist; the service is Type=oneshot with EnvironmentFile and an ExecStart pointing at rate_limit_cleanup.sh; the new timer uses 00:15 and the retention timer now uses explicit 00:00; the new timer has [Install] WantedBy=timers.target.</acceptance_criteria>
  <done>The rate-limit cleanup is schedulable via systemd and staggered 15 minutes after retention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test rate_limit_cleanup.sh deletion behavior + unit-file correctness</name>
  <files>backend/tests/test_rate_limit_cleanup_script.py</files>
  <read_first>backend/tests/conftest.py (temp_db fixture, 114-153), backend/scripts/rate_limit_cleanup.sh (deletes where unixepoch('now') - unixepoch(last_refill) >= 24h), backend/app/schema.sql (rate_limit_buckets table columns), .planning/phases/04-data-management/04-PATTERNS.md (subprocess-against-temp_db idiom)</read_first>
  <behavior>
    - Test A (deletes stale, keeps fresh): seed rate_limit_buckets with one bucket whose last_refill is ~48h ago and one whose last_refill is now; run rate_limit_cleanup.sh via subprocess with BMTC_DB_PATH set; assert returncode == 0, the stale bucket is gone, the fresh bucket remains.
    - Test B (empty table no-op): run against a temp_db with zero buckets; assert returncode == 0 and stdout reports 0 remaining, no error.
    - Test C (unit-file correctness, static): read backend/deploy/bmtc-rate-limit-cleanup.service and .timer and backend/deploy/bmtc-retention.timer as text; assert the service is Type=oneshot with an ExecStart referencing rate_limit_cleanup.sh and EnvironmentFile=/etc/bmtc-api/env; assert the new timer OnCalendar is 00:15 and retention timer OnCalendar is 00:00 (distinct, D-09); assert the new timer has WantedBy=timers.target.
  </behavior>
  <action>Create backend/tests/test_rate_limit_cleanup_script.py. Use the temp_db fixture and subprocess.run(["bash", "backend/scripts/rate_limit_cleanup.sh"], env={"BMTC_DB_PATH": db_path, "PATH": "/usr/bin:/bin"}, capture_output=True, text=True). Seed rows with INSERT into rate_limit_buckets using the actual column set from schema.sql (confirm last_refill's type/format so the unixepoch() delta in the script matches the seeded value — the script computes unixepoch('now') - unixepoch(last_refill)). Test C reads the three unit files from backend/deploy/ and asserts the directives above via plain string checks. This is characterization coverage — the script already works, so the deletion tests are expected to pass immediately (not RED→GREEN). Confirm the invocation path to rate_limit_cleanup.sh resolves from the repo root the tests run in.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_rate_limit_cleanup_script.py -x</automated>
  </verify>
  <acceptance_criteria>All three tests pass. Deletion tests prove the 24h TTL boundary; the static test proves the unit files exist with the correct staggered schedule and ExecStart. Full suite: `cd backend && uv run pytest -n auto --dist loadfile -q` shows no new regressions.</acceptance_criteria>
  <done>DATA-02 met: rate_limit_cleanup.sh is scheduled via a systemd timer whose files exist and parse, and its deletion behavior is under test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| systemd timer → rate_limit_cleanup.sh | Timer-triggered local script; no external input |
| script → bmtc.db | Bounded DELETE against rate_limit_buckets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Denial of Service | Two timers writing the single-writer DB simultaneously | low | mitigate | D-09 staggers OnCalendar to 00:00 vs 00:15; WAL mode tolerates concurrent writes regardless (defense in depth). |
| T-04-04 | Tampering | systemd unit ExecStart path | low | accept | Unit files are repo-controlled and deployed by the operator; ExecStart points at a fixed repo script path, no dynamic input. |

No package installs this phase — Package Legitimacy Audit N/A (RESEARCH.md).
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_rate_limit_cleanup_script.py -x` — green
- `cd backend && uv run pytest -n auto --dist loadfile -q` — no regressions
- Optional (CI/host with systemd): `systemd-analyze verify backend/deploy/bmtc-rate-limit-cleanup.service` parses without fatal errors
</verification>

<success_criteria>
DATA-02 met: the rate_limit_buckets cleanup runs on a systemd timer (unit files exist, enabled via WantedBy=timers.target, staggered from retention), and the cleanup script's TTL-bounded deletion is verified by an automated test.
</success_criteria>

<output>
Create `.planning/phases/04-data-management/04-02-SUMMARY.md` when done.
</output>
