---
phase: 05-quality-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_bootstrap.py
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "Running `uv run pytest tests/test_bootstrap.py` passes as part of the normal suite (no opt-in marker)"
    - "A fresh bootstrap creates exactly the 17 domain tables and 3 views defined in schema.sql — a silently renamed/dropped table fails the test"
    - "GTFS metadata is populated after bootstrapping from the mini fixture (a gtfs_version row exists)"
    - "PRAGMA foreign_key_check returns zero violations with foreign key enforcement explicitly enabled"
  artifacts:
    - "backend/tests/test_bootstrap.py"
  key_links:
    - "test reads the literal table/view names from the verified 17+3 list, NOT the stale '11 tables' figure in ROADMAP/CLAUDE.md"
    - "FK check runs only after PRAGMA foreign_keys = ON (SQLite defaults it OFF per-connection)"
---

<objective>
Create `backend/tests/test_bootstrap.py` (OPS-02): an automated smoke test that proves a fresh
bootstrap produces a structurally correct database — all domain tables and views exist by literal
name, GTFS metadata is populated, and there are zero foreign-key violations.

Purpose: Catch schema drift (a table silently renamed, dropped, or a broken FK) on every commit,
cheaply, using the small `mini_gtfs.zip` fixture (D-07) rather than the 1.46M-row production `bmtc.zip`.
Output: `backend/tests/test_bootstrap.py` with three test functions, running inside the normal
`uv run pytest -n auto --dist loadfile` suite (D-08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-operations/05-CONTEXT.md
@.planning/phases/05-quality-operations/05-RESEARCH.md
@.planning/phases/05-quality-operations/05-PATTERNS.md

@backend/app/schema.sql
@backend/app/db.py
@backend/tests/conftest.py
@backend/tests/test_gtfs_update.py
</context>

## Artifacts this phase produces (Plan 01)

- New file: `backend/tests/test_bootstrap.py`
- New test functions:
  - `test_bootstrap_creates_all_tables_and_views` — literal 17-table / 3-view assertion against `sqlite_master`
  - `test_bootstrap_populates_gtfs_metadata` — asserts a `gtfs_version` row exists in `gtfs_metadata` after a mini-fixture bootstrap
  - `test_bootstrap_foreign_keys_valid` — `PRAGMA foreign_key_check` returns `[]` with FK enforcement ON
- New module-level constants inside the test file:
  - `EXPECTED_TABLES` (frozenset/set of 17 names)
  - `EXPECTED_VIEWS` (set of 3 names)

<tasks>

<task type="auto">
  <name>Task 1: Literal table/view existence test against sqlite_master (D-09)</name>
  <read_first>
    - backend/app/schema.sql — authoritative source of table/view names (CREATE TABLE / CREATE VIEW statements)
    - backend/app/db.py — `init_db(db_path)` seeds schema + 192 time_bins + creates the `schema_migrations` bookkeeping table
    - backend/tests/conftest.py:90-111 — `in_memory_db` fixture (schema-load + `PRAGMA foreign_keys = ON` pattern to mirror)
    - backend/tests/test_gtfs_update.py:34 — `GTFS_TABLES = [...]` literal-list style to mirror
    - .planning/phases/05-quality-operations/05-RESEARCH.md — Pitfall 1 (verified 17-table/3-view list) and Pattern 6
  </read_first>
  <files>backend/tests/test_bootstrap.py</files>
  <action>
    Create `backend/tests/test_bootstrap.py`. Define two module-level constants using the VERIFIED
    literal names from RESEARCH.md Pitfall 1 — NOT the stale "11 tables" figure from ROADMAP/CLAUDE.md
    (research correction #1: the live schema has 17 tables + 3 views):
    EXPECTED_TABLES = {agency, routes, stops, calendar, trips, stop_times, gtfs_metadata (GTFS static, 7);
    segments, time_bins, segment_stats, dwell_stats, rides, ride_segments (learning, 6);
    idempotency_keys, device_buckets, rejection_log, rate_limit_buckets (audit, 4)} = 17 names.
    EXPECTED_VIEWS = {route_summary, stop_summary, segment_learning_progress} = 3 names.
    Write `test_bootstrap_creates_all_tables_and_views(tmp_path)`: call `from app.db import init_db;
    init_db(str(tmp_path / "bootstrap_test.db"))`, open the DB with `sqlite3.connect`, query
    `SELECT type, name FROM sqlite_master WHERE type IN ('table','view') AND name NOT LIKE 'sqlite_%'
    AND name != 'schema_migrations'`, and assert the collected table-name set == EXPECTED_TABLES and
    view-name set == EXPECTED_VIEWS. Explicitly exclude `schema_migrations` in the WHERE clause AND
    document in a code comment WHY it is excluded (per D-09 / RESEARCH Open Question 1: it is a
    migration-framework bookkeeping table created by `init_db()`, not part of the domain schema in
    schema.sql). Close the connection in a `finally`. Do NOT hardcode any bare count assertion
    (no `== 11`, no `== 17`) — assert against the literal name sets so a rename or drop is caught.
  </action>
  <acceptance_criteria>
    - `backend/tests/test_bootstrap.py` exists and imports cleanly
    - EXPECTED_TABLES has exactly 17 names and EXPECTED_VIEWS has exactly 3 names, matching the RESEARCH Pitfall 1 list verbatim
    - The `sqlite_master` query filters out `sqlite_%` internal tables AND `schema_migrations`, with a comment explaining the `schema_migrations` exclusion
    - `cd backend && uv run pytest tests/test_bootstrap.py::test_bootstrap_creates_all_tables_and_views -v` passes
    - No literal integer count assertion (`== 11` / `== 14` / `== 17`) appears anywhere in the file
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_bootstrap.py::test_bootstrap_creates_all_tables_and_views -v</automated>
  </verify>
  <done>The bootstrap-produced DB is asserted to contain exactly the 17 named domain tables and 3 named views; a renamed/dropped table fails the test.</done>
</task>

<task type="auto">
  <name>Task 2: GTFS-metadata-populated + foreign-key-integrity tests (D-10)</name>
  <read_first>
    - backend/app/gtfs_bootstrap.py:11 (`parse_gtfs(gtfs_path, db_conn)`) and :348 (`store_metadata`, inserts `gtfs_version` key into `gtfs_metadata` from feed_info.txt)
    - backend/app/db.py — `init_db(db_path)` and `get_connection(db_path)` context manager
    - backend/tests/fixtures/mini_gtfs.zip — the fixture to bootstrap from (contains feed_info.txt, so gtfs_version WILL be populated)
    - backend/tests/conftest.py:139 — `temp_db` fixture showing the `PRAGMA foreign_keys = ON` precedent
    - .planning/phases/05-quality-operations/05-RESEARCH.md — Pattern 5 (FK check) and Pitfall 7 (FK enforcement is OFF by default per-connection)
  </read_first>
  <files>backend/tests/test_bootstrap.py</files>
  <action>
    Add two more test functions to `backend/tests/test_bootstrap.py`.
    (a) `test_bootstrap_populates_gtfs_metadata(tmp_path)`: `init_db()` a scratch DB path, then bootstrap
    GTFS by opening `app.db.get_connection(db_path)` and calling
    `app.gtfs_bootstrap.parse_gtfs(str(MINI_GTFS_ZIP), conn)` where
    `MINI_GTFS_ZIP = Path(__file__).parent / "fixtures" / "mini_gtfs.zip"`. Then query
    `SELECT value FROM gtfs_metadata WHERE key = 'gtfs_version'` and assert exactly one row exists and
    its value is a non-empty string (the mini fixture's feed_info.txt supplies a feed_version).
    (b) `test_bootstrap_foreign_keys_valid(tmp_path)`: `init_db()` a scratch DB, bootstrap the mini
    fixture as in (a), open a fresh `sqlite3.connect`, run `conn.execute("PRAGMA foreign_keys = ON")`
    FIRST (REQUIRED — the check is a documented no-op without it; SQLite defaults FK enforcement OFF
    per-connection and `app.db.get_connection()` never sets it), then assert
    `conn.execute("PRAGMA foreign_key_check").fetchall() == []`. Close connections in `finally`.
    Reuse the existing `in_memory_db`/`temp_db` FK-ON idiom from conftest.py rather than inventing new setup.
  </action>
  <acceptance_criteria>
    - `test_bootstrap_populates_gtfs_metadata` asserts a single non-empty `gtfs_version` row after bootstrapping mini_gtfs.zip
    - `test_bootstrap_foreign_keys_valid` executes `PRAGMA foreign_keys = ON` BEFORE `PRAGMA foreign_key_check`, and asserts the result equals `[]`
    - Both tests bootstrap from `backend/tests/fixtures/mini_gtfs.zip`, never from the real `bmtc.zip` (D-07)
    - `cd backend && uv run pytest tests/test_bootstrap.py -v` passes all three tests
    - Full suite baseline is not regressed: `cd backend && uv run pytest -n auto --dist loadfile` shows no NEW failures beyond the known 6 pre-existing
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run pytest tests/test_bootstrap.py -v</automated>
  </verify>
  <done>A fresh mini-fixture bootstrap is proven to populate GTFS metadata and to have zero FK violations with enforcement explicitly on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → scratch SQLite DB | Test creates and inspects a throwaway `tmp_path` DB; no untrusted input, no production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | test operates on real `bmtc_dev.db` instead of a scratch DB | low | mitigate | All tests use pytest `tmp_path`-scoped DBs and the `mini_gtfs.zip` fixture (D-07); never touch `bmtc_dev.db` or the real `bmtc.zip` |
| T-05-02 | Denial of Service | slow test blocks the every-commit suite | low | accept | Mini fixture bootstrap is sub-second; this is precisely why D-07 rejects the 1.46M-row real GTFS |

*No new packages introduced (RESEARCH Package Legitimacy Audit: zero new dependencies), so no supply-chain (T-*-SC) checkpoint applies.*
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_bootstrap.py -v` → 3 passing tests
- `cd backend && uv run pytest -n auto --dist loadfile` → no new failures vs. the 6 pre-existing baseline
- Grep confirms no bare count assertion: the file asserts literal name sets, not `len(...) == N`
</verification>

<success_criteria>
OPS-02 satisfied: `backend/tests/test_bootstrap.py` exists and passes in the normal suite, verifying (1) the 17 tables + 3 views exist by literal name, (2) GTFS metadata is populated, (3) `PRAGMA foreign_key_check` returns zero violations with FK enforcement on.
</success_criteria>

<output>
Create `.planning/phases/05-quality-operations/05-01-SUMMARY.md` when done
</output>
