---
phase: 05-quality-operations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/perf/load_test.py
  - backend/tests/perf/results.txt
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "A standalone script at backend/tests/perf/load_test.py drives >=20 concurrent clients against a running server"
    - "Each simulated POST client uses a distinct device_bucket, exercising the real RateLimitMiddleware per-bucket path"
    - "POST /v1/ride_summary and GET /v1/eta are load-tested in separate runs against their own targets (200ms / 100ms)"
    - "p50/p99 are computed in pure Python (nearest-rank) and written to backend/tests/perf/results.txt"
    - "The script targets a scratch DB and localhost only — never bmtc_dev.db or a deployed instance"
  artifacts:
    - "backend/tests/perf/load_test.py"
    - "backend/tests/perf/results.txt"
  key_links:
    - "seed step inserts segments + all-192-bin segment_stats directly (mirrors conftest db_with_test_segment) — generate_sample_data.py IDs would 422"
    - "payload uses observed_at_utc ISO-8601, timestamps in the past, to avoid future-timestamp 422 and deprecation-header noise"
    - "load_test.py is NOT prefixed test_ so pytest does not collect it (D-06)"
---

<objective>
Deliver OPS-01: a manual, local-only load test proving POST /v1/ride_summary p99 < 200ms and
GET /v1/eta p99 < 100ms under >=20 concurrent clients, with results captured in a committed artifact.

Purpose: Verify the performance targets stated in CLAUDE.md/PROJECT.md under realistic concurrency
(each client its own `device_bucket`, real `RateLimitMiddleware` path — D-04), isolating each endpoint
in its own run (D-05) for clean p99 attribution.
Output: `backend/tests/perf/load_test.py` (standalone asyncio/httpx script, NOT a pytest test — D-06)
and `backend/tests/perf/results.txt` (evidence of a manual run — D-03).

Applies RESEARCH correction #4: the seed step directly inserts `segments` (and, for the GET run,
`segment_stats` across all 192 bins) rows mirroring conftest's `db_with_test_segment` — it does NOT
rely on `generate_sample_data.py`'s synthetic IDs (which 422) or on GET auto-seeding (it 404s on a
missing segment×bin row).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-operations/05-CONTEXT.md
@.planning/phases/05-quality-operations/05-RESEARCH.md
@.planning/phases/05-quality-operations/05-PATTERNS.md

@backend/scripts/generate_sample_data.py
@backend/tests/conftest.py
@backend/app/rate_limit.py
</context>

## Artifacts this phase produces (Plan 03)

- New file: `backend/tests/perf/load_test.py` (standalone; `if __name__ == "__main__"` entry, argparse)
  - `seed_scratch_db(db_path)` — inserts a `PERF_ROUTE` segment + all-192-bin `segment_stats` rows
  - `percentile(samples, p)` — pure-Python nearest-rank percentile
  - `post_client(...)` / `eta_client(...)` — async per-client request loops (httpx.AsyncClient)
  - `run(endpoint, n_clients, n_requests)` — `asyncio.gather` fan-out
  - `main()` — argparse (`--endpoint {post|eta}`, `--clients`, `--requests`, `--seed`), writes results.txt
- New file: `backend/tests/perf/results.txt` — appended run lines: `<iso> endpoint=... clients=... requests=... p50_ms=... p99_ms=... target_ms=... PASS=...`

<tasks>

<task type="auto">
  <name>Task 1: Build the standalone asyncio/httpx load test script</name>
  <read_first>
    - backend/scripts/generate_sample_data.py — script shape (docstring, `if __name__ == "__main__"`); note its synthetic IDs and timestamp_utc field are NOT reusable as-is (Pitfalls 4/5)
    - backend/tests/conftest.py:156-196 — `db_with_test_segment` fixture: the exact segments + 192-bin segment_stats seeding to replicate directly via sqlite3
    - backend/tests/conftest.py:265-309 — `sample_ride_data` factory: correct v1 payload shape (route_id, direction_id, device_bucket, segments[{from_stop_id,to_stop_id,duration_sec,observed_at_utc,mapmatch_conf}])
    - backend/app/rate_limit.py — RateLimitMiddleware per-device_bucket token check the load must work with (D-04)
    - .planning/phases/05-quality-operations/05-RESEARCH.md — Code Examples (full load_test.py skeleton), Pattern 1 (httpx concurrency), Pattern 2 (percentile), Pitfalls 4/5/6
  </read_first>
  <files>backend/tests/perf/load_test.py</files>
  <action>
    Create `backend/tests/perf/load_test.py` as a standalone script (NOT prefixed `test_`; D-06 — pytest
    must not collect it). Module docstring documents the two-terminal usage: start a scratch-DB-backed
    server (`BMTC_DB_PATH=/tmp/perf.db BMTC_API_KEY=perf-test-key uv run uvicorn app.main:app --port 8001 &`),
    then run this script. Hardcode local-only defaults BASE_URL="http://127.0.0.1:8001",
    API_KEY="perf-test-key", DB_PATH="/tmp/perf.db" — never read BMTC_DB_PATH/BASE_URL from the ambient
    environment (T-05-06: prevents an accidental run against a real deployment).
    Implement `seed_scratch_db(db_path)`: `sqlite3.connect`, `INSERT OR IGNORE` one segment
    (route_id='PERF_ROUTE', direction_id=0, from_stop_id='PERF_STOP_A', to_stop_id='PERF_STOP_B'),
    fetch its segment_id, then `INSERT OR IGNORE` `segment_stats` for `bin_id in range(192)` with
    schedule_mean=300.0, n=5, welford_mean=300.0, welford_m2=100.0 (n>0 and welford populated so
    GET /v1/eta returns 200 with a learned value, not a 404) — mirroring conftest's `db_with_test_segment`.
    Commit and close. Implement `percentile(samples, p)` using nearest-rank:
    `idx = max(0, min(math.ceil((p/100)*len(ordered)) - 1, len(ordered) - 1))` on `sorted(samples)`.
    Implement async `post_client(client_id, n_requests, latencies)`: per client a distinct
    `device_bucket = hashlib.sha256(f"perf-client-{client_id}".encode()).hexdigest()` (D-04), its own
    `httpx.AsyncClient(base_url=BASE_URL, timeout=10.0)`, POSTing the v1 payload with
    `observed_at_utc = datetime.now(timezone.utc).isoformat().replace("+00:00","Z")` (past/now, never
    future — Pitfall 4) and Bearer auth; record `(time.perf_counter()-start)*1000`; assert status in
    (200, 429) (429 is a valid expected outcome under D-04, not a bug). Implement async
    `eta_client(...)`: GET /v1/eta with params route_id=PERF_ROUTE, direction_id=0, from_stop_id=PERF_STOP_A,
    to_stop_id=PERF_STOP_B; assert status 200. Implement `run(endpoint, n_clients, n_requests)` using
    `asyncio.gather` over the chosen client fn. Implement `main()` with argparse
    (`--endpoint {post,eta}` required, `--clients` default 20, `--requests` default 25, `--seed` flag);
    when `--seed`, call `seed_scratch_db(DB_PATH)` first; run; compute p50/p99; set
    `target_ms = 200 if post else 100`; print and append one line to `backend/tests/perf/results.txt`:
    `<iso> endpoint=... clients=... requests=... p50_ms=... p99_ms=... target_ms=... PASS=<p99<target>`.
    Use pace-per-round-trip (each client's requests are sequential awaits, not a tight burst) per
    RESEARCH Pitfall 6 / Assumption A3 for a defensible steady-rate p99.
  </action>
  <acceptance_criteria>
    - `backend/tests/perf/load_test.py` exists, is NOT prefixed `test_`, and is not collected by pytest (`uv run pytest --collect-only` does not list it)
    - `percentile([1..100], 99) == 99` and `percentile([1..100], 50) == 50` (nearest-rank), verifiable via direct import
    - `seed_scratch_db` inserts one segment + 192 segment_stats rows with n>0 (so GET /v1/eta returns 200)
    - POST payload uses `observed_at_utc` ISO-8601 (not `timestamp_utc`) and each client derives a distinct SHA256 `device_bucket`
    - BASE_URL/API_KEY/DB_PATH are hardcoded local defaults, never read from the ambient environment
    - `cd backend && uv run python -c "import sys; sys.path.insert(0,'tests/perf'); import load_test as L; assert L.percentile(list(range(1,101)),99)==99 and L.percentile(list(range(1,101)),50)==50; print('OK')"` prints OK
  </acceptance_criteria>
  <verify>
    <automated>cd backend && uv run python -c "import sys; sys.path.insert(0,'tests/perf'); import load_test as L; assert L.percentile(list(range(1,101)),99)==99; assert L.percentile(list(range(1,101)),50)==50; assert L.percentile([5.0],99)==5.0; print('OK')"</automated>
  </verify>
  <done>A standalone, dependency-free load-test script exists with correct seeding, per-client device buckets, ISO-8601 timestamps, and a verified pure-Python percentile.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the two load runs and commit results.txt evidence</name>
  <read_first>
    - backend/tests/perf/load_test.py — the script from Task 1 and its module-docstring usage instructions
    - .planning/phases/05-quality-operations/05-RESEARCH.md — Code Examples (usage block), Pitfall 6 (SQLite single-writer p99 interpretation)
    - .planning/phases/05-quality-operations/05-CONTEXT.md — D-03 (results.txt committed as evidence, NOT a CI gate), D-05 (separate runs)
  </read_first>
  <files>backend/tests/perf/results.txt</files>
  <action>
    Perform (or, if no runtime is available in this environment, have the operator perform) the manual
    load runs, then commit `backend/tests/perf/results.txt` as evidence (D-03). Steps: (1) start a
    scratch-DB server: `BMTC_DB_PATH=/tmp/perf.db BMTC_API_KEY=perf-test-key uv run uvicorn app.main:app
    --port 8001 &`; (2) `cd backend && uv run python tests/perf/load_test.py --seed --endpoint post
    --clients 20 --requests 25`; (3) `cd backend && uv run python tests/perf/load_test.py --endpoint eta
    --clients 20 --requests 25`; (4) stop the server. Confirm the two appended lines in results.txt show
    p99_ms below their targets (POST<200, GET<100) with PASS=True. If p99 misses the POST target,
    annotate results.txt noting SQLite single-writer WAL serialization under burst as the documented
    architectural cause (RESEARCH Pitfall 6), not a code regression — do NOT change the DB engine
    (out of scope). Ensure results.txt is committed to git as the OPS-01 evidence artifact.
    This run is manual/local-only and is NOT wired into CI (D-03).
  </action>
  <acceptance_criteria>
    - `backend/tests/perf/results.txt` exists and contains at least one `endpoint=post` line and one `endpoint=eta` line
    - Each line records clients (>=20), request count, p50_ms, p99_ms, target_ms, and a PASS flag
    - results.txt is committed to git
    - The load test is NOT referenced by `.github/workflows/ci.yml` (D-03 — manual only)
  </acceptance_criteria>
  <verify>
    <automated>test -f backend/tests/perf/results.txt && grep -q "endpoint=post" backend/tests/perf/results.txt && grep -q "endpoint=eta" backend/tests/perf/results.txt && echo OK</automated>
    <human-check>Confirm the POST run line shows p99_ms < 200 and the GET run line shows p99_ms < 100 (PASS=True), captured under >=20 concurrent clients. If a target is missed, confirm the annotation attributes it to SQLite single-writer serialization (RESEARCH Pitfall 6), not a code regression.</human-check>
  </verify>
  <done>Two load runs (POST and GET, separate) have been executed against a scratch-DB server and their p50/p99 results are committed in results.txt as OPS-01 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| load script → target server/DB | The script chooses which DB and URL to hit; must never point at production data or a live deployment |
| load client → API | Synthetic authenticated POSTs cross the real auth + rate-limit boundary (intentional, D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering / Denial of Service | load_test.py accidentally targeting `bmtc_dev.db` or a deployed instance | high | mitigate | Hardcode `DB_PATH=/tmp/perf.db` and `BASE_URL=127.0.0.1:8001` as local defaults; never read `BMTC_DB_PATH`/`BASE_URL` from the ambient shell env (D-02) |
| T-05-07 | Spoofing | load uses a single shared device_bucket, bypassing real rate-limit behavior | low | mitigate | Each client derives a distinct SHA256 device_bucket (D-04), exercising the real per-bucket token path |

*No new packages introduced (RESEARCH Package Legitimacy Audit: zero new dependencies — httpx/asyncio/sqlite3 already present), so no supply-chain (T-*-SC) checkpoint applies.*
</threat_model>

<verification>
- `cd backend && uv run pytest --collect-only -q | grep -c 'load_test'` → 0 (script is not collected as a pytest test, D-06)
- `percentile` self-check (Task 1 automated) prints OK
- `backend/tests/perf/results.txt` contains a POST line and a GET line, committed to git
- Human-check: p99 targets met (POST<200ms, GET<100ms) under >=20 clients, or the miss is annotated per Pitfall 6
</verification>

<success_criteria>
OPS-01 satisfied: a reproducible standalone load test demonstrates POST p99 < 200ms and GET p99 < 100ms under >=20 concurrent clients, with `backend/tests/perf/results.txt` committed as evidence and the test kept out of CI (D-03).
</success_criteria>

<output>
Create `.planning/phases/05-quality-operations/05-03-SUMMARY.md` when done
</output>
